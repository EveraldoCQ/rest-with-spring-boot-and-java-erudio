spring:
  application:
    name: api-gateway
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rest_with_spring_everaldo?useTimezone=true&serverTimezone=UTC
    username: root
    password: TRTMySQL-01
  jpa:
    open-in-view: false
    #open-in-view: true #Padrão
    hibernate:
      ddl-auto: update
    #properties:
      #hibernate:
        #dialect: org.hibernate.dialect.MySQLDialect
    show-sql: false